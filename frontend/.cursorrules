### รูปแบบโค้ด
- ใช้ **TypeScript**
- ใช้ **ESLint** และ **Prettier**
- ใช้ **ภาษาไทย** ใน comments
- ใช้ **semantic commit messages**
- ใช้ **heroui** ทุกหน้า
- ใช้ **tailwindcss** ทุกหน้า

front เป็นส่วนหนึ่งของ microservice ที่ทำงานร่วมกับ backend ผ่าน API Gateway โดยตรง

ไม่ต้องสร้างไฟล์ทดสอบที่ frontend หรือ backend