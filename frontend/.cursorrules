# Frontend Service - Cursor Rules
# ระบบ Portal RPP - Frontend Service

## 🎯 เป้าหมายหลัก
- **Frontend Service** เป็นส่วนติดต่อผู้ใช้ในระบบ Microservices
- ใช้ **Next.js + TypeScript + HeroUI** เป็นเทคโนโลยีหลัก
- รองรับ **ภาษาไทย** และ **การเชื่อมต่อกับ API Gateway**

## 🏗️ สถาปัตยกรรม Frontend

### 📁 โครงสร้างโปรเจค
```
frontend/
├── app/                    # Next.js App Router
│   ├── dashboard/         # แดชบอร์ดหลัก
│   ├── login/            # หน้าเข้าสู่ระบบ
│   └── layout.tsx        # Layout หลัก
├── components/            # Reusable Components
│   ├── navbar.tsx        # Navigation Bar
│   ├── footer.tsx        # Footer
│   └── icons.tsx         # Icon Components
├── config/               # Configuration Files
│   ├── site.ts          # Site Configuration
│   └── fonts.ts         # Font Configuration
├── styles/               # Global Styles
├── types/                # TypeScript Types
└── utils/                # Utility Functions
```

### 🔗 การเชื่อมต่อกับ Backend
- **เรียก API ผ่าน API Gateway เท่านั้น** - ห้ามเรียก service โดยตรง
- ใช้ URL: `http://localhost:3001` สำหรับทุก API calls
- ตัวอย่าง: `/api/auth/login` → `http://localhost:3001/api/auth/login`
- **ห้ามเรียก**: `http://localhost:3002`, `http://localhost:3003`, `http://localhost:3004` โดยตรง

## 🎨 UI/UX Guidelines

### 🎯 Design System
- ใช้ **HeroUI** เป็น component library หลัก
- ใช้ **Tailwind CSS** สำหรับ styling
- ใช้ **Font Prompt** สำหรับภาษาไทย
- ใช้ **Color Scheme**: Blue (#2563eb) เป็น primary color

### 📱 Responsive Design
- **Mobile First** approach
- รองรับ **Desktop, Tablet, Mobile**
- ใช้ **Grid System** และ **Flexbox**
- **Touch-friendly** สำหรับ mobile devices

### 🌐 Internationalization
- รองรับ **ภาษาไทย** เป็นหลัก
- ใช้ **th-TH locale** สำหรับ date/time formatting
- **RTL support** สำหรับอนาคต

## 🔧 Development Guidelines

### 📝 Code Standards
- ใช้ **TypeScript** ทุกไฟล์
- ใช้ **ESLint** และ **Prettier**
- ใช้ **ภาษาไทย** ใน comments และ strings
- ใช้ **semantic commit messages**

### 🧩 Component Architecture
- **Atomic Design** principles
- **Reusable components** ที่สามารถใช้ซ้ำได้
- **Props interface** ที่ชัดเจน
- **Error boundaries** สำหรับ error handling

### 🎯 Performance
- **Code splitting** ด้วย Next.js
- **Image optimization** ด้วย Next.js Image
- **Lazy loading** สำหรับ components ที่ไม่จำเป็น
- **Bundle size optimization**

## 🔐 Security Guidelines

### 🛡️ Authentication & Authorization
- **JWT token** management
- **Protected routes** สำหรับ authenticated users
- **Role-based access control** (RBAC)
- **Session management** ที่ปลอดภัย

### 🔒 Data Protection
- **HTTPS only** ใน production
- **Input validation** ทุก form
- **XSS protection** ด้วย proper escaping
- **CSRF protection** สำหรับ forms

## 📊 State Management

### 🎯 State Strategy
- ใช้ **React Context** สำหรับ global state
- ใช้ **useState/useReducer** สำหรับ local state
- ใช้ **SWR/React Query** สำหรับ server state
- **Optimistic updates** สำหรับ UX ที่ดี

### 🔄 Data Flow
- **Unidirectional data flow**
- **Props drilling** หลีกเลี่ยงด้วย Context
- **Event-driven** architecture
- **Error handling** ที่ครอบคลุม

## 🧪 Testing Strategy

### 🎯 Testing Levels
- **Unit tests** สำหรับ components
- **Integration tests** สำหรับ API calls
- **E2E tests** สำหรับ user flows
- **Accessibility tests** สำหรับ a11y

### 🛠️ Testing Tools
- **Jest** สำหรับ unit testing
- **React Testing Library** สำหรับ component testing
- **Cypress** สำหรับ E2E testing
- **Lighthouse** สำหรับ performance testing

## 🚀 Deployment Guidelines

### 📦 Build Process
- **Environment-specific** builds
- **Asset optimization** และ compression
- **Source maps** สำหรับ debugging
- **Bundle analysis** เพื่อ optimize size

### 🌍 Environment Configuration
- **Development**: `http://localhost:3000`
- **API Gateway**: `http://localhost:3001`
- **Environment variables** สำหรับ configuration
- **Feature flags** สำหรับ gradual rollout

## 📱 PWA Features

### 🎯 Progressive Web App
- **Service Worker** สำหรับ offline support
- **Manifest.json** สำหรับ app-like experience
- **Push notifications** สำหรับ user engagement
- **Install prompt** สำหรับ home screen

## 🔍 Monitoring & Analytics

### 📊 Performance Monitoring
- **Core Web Vitals** tracking
- **Error tracking** ด้วย error boundaries
- **User analytics** สำหรับ UX improvement
- **Performance budgets** เพื่อ maintain speed

### 🚨 Error Handling
- **Global error boundary** สำหรับ unhandled errors
- **User-friendly error messages** ภาษาไทย
- **Error logging** สำหรับ debugging
- **Graceful degradation** เมื่อ API ล้มเหลว

## 🎯 Best Practices

### 📝 Code Quality
- **DRY principle** - Don't Repeat Yourself
- **KISS principle** - Keep It Simple, Stupid
- **SOLID principles** สำหรับ component design
- **Clean Code** practices

### 🎨 Accessibility
- **WCAG 2.1 AA** compliance
- **Keyboard navigation** support
- **Screen reader** compatibility
- **Color contrast** ที่เหมาะสม

### 🔧 Performance
- **Lazy loading** สำหรับ images และ components
- **Code splitting** เพื่อลด bundle size
- **Caching strategies** สำหรับ API responses
- **Optimized images** และ assets

## 🚫 Anti-patterns to Avoid

### ❌ Common Mistakes
- **Direct API calls** ไปยัง backend services
- **Hard-coded URLs** ใน components
- **Large bundle sizes** ที่ไม่จำเป็น
- **Poor error handling** ที่ไม่ครอบคลุม
- **Inconsistent styling** ระหว่าง components
- **No loading states** สำหรับ async operations

## 📚 Documentation

### 📖 Code Documentation
- **JSDoc comments** สำหรับ functions
- **README files** สำหรับ complex components
- **API documentation** สำหรับ external APIs
- **Component storybook** สำหรับ UI components

### 🎯 User Documentation
- **User guides** ภาษาไทย
- **Help tooltips** ใน UI
- **Error messages** ที่ชัดเจน
- **Onboarding flow** สำหรับ new users

## 🔄 Continuous Improvement

### 📈 Metrics to Track
- **Page load times** และ performance
- **User engagement** และ retention
- **Error rates** และ user feedback
- **Accessibility scores** และ compliance

### 🎯 Regular Reviews
- **Code reviews** สำหรับทุก PR
- **Performance audits** ทุกเดือน
- **Security audits** ทุกไตรมาส
- **User feedback** collection และ analysis

## 🎯 Success Criteria

### ✅ Quality Metrics
- **Lighthouse score** > 90
- **Bundle size** < 500KB
- **Load time** < 3 seconds
- **Error rate** < 1%

### 🎯 User Experience
- **Intuitive navigation** และ user flows
- **Fast response times** สำหรับ user interactions
- **Consistent design** ระหว่าง pages
- **Accessible** สำหรับทุก users

---

## 🚀 Quick Start Commands

```cmd
# Development
npm run dev              # รัน development server
npm run build           # Build สำหรับ production
npm run start           # รัน production server

# Code Quality
npm run lint            # ตรวจสอบ code style
npm run lint:fix        # แก้ไข code style อัตโนมัติ
npm run type-check      # ตรวจสอบ TypeScript types
npm run format          # จัดรูปแบบโค้ดด้วย Prettier

# Testing
npm run test            # รัน unit tests
npm run test:watch      # รัน tests ใน watch mode
npm run test:coverage   # รัน tests พร้อม coverage report
```

### Windows Development Commands
- **ใช้คำสั่ง cmd เป็นมาตรฐาน** สำหรับการพัฒนาใน Windows
- ใช้ `cmd` แทน `bash` หรือ `powershell` สำหรับคำสั่ง terminal
- ใช้ `dir` แทน `ls` สำหรับการแสดงรายการไฟล์
- ใช้ `copy` แทน `cp` สำหรับการคัดลอกไฟล์
- ใช้ `move` แทน `mv` สำหรับการย้ายไฟล์
- ใช้ `del` แทน `rm` สำหรับการลบไฟล์
- ใช้ `mkdir` สำหรับการสร้างโฟลเดอร์
- ใช้ `cd` สำหรับการเปลี่ยนไดเรกทอรี
- ใช้ `netstat -ano | findstr :PORT` สำหรับการตรวจสอบ port ที่ใช้งาน
- ใช้ `taskkill /PID <PID>` สำหรับการหยุด process
- ใช้ `tasklist` สำหรับการแสดงรายการ processes
- ใช้ `ipconfig` สำหรับการตรวจสอบ network configuration
- ใช้ `ping` สำหรับการทดสอบการเชื่อมต่อ
- ใช้ `nslookup` สำหรับการตรวจสอบ DNS
- ใช้ `tracert` แทน `traceroute` สำหรับการติดตามเส้นทาง
- ใช้ `systeminfo` สำหรับข้อมูลระบบ
- ใช้ `ver` สำหรับเวอร์ชันของ Windows
- ใช้ `echo %PATH%` สำหรับการแสดง PATH environment variable
- ใช้ `set` สำหรับการแสดง environment variables
- ใช้ `set VARIABLE=value` สำหรับการตั้งค่า environment variable
- ใช้ `cls` สำหรับการล้างหน้าจอ
- ใช้ `help` สำหรับการแสดงคำสั่งที่ใช้ได้
- ใช้ `/?` หลังคำสั่งเพื่อดู help (เช่น `dir /?`)

## 🌐 URLs สำหรับการพัฒนา
- **Frontend**: http://localhost:3000
- **API Gateway**: http://localhost:3001
- **Auth Service**: http://localhost:3002
- **User Service**: http://localhost:3003
- **Database Service**: http://localhost:3004

---

*อัปเดตล่าสุด: 2024*
*เวอร์ชัน: 1.0.0*
*ผู้พัฒนา: ฝ่ายวิชาการ โรงพยาบาลราชพิพัฒน์* 