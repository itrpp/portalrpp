import HomeClient from "./HomeClient";

/**
 * หน้า Home – Server Component
 * แสดง UI ผ่าน HomeClient (Client Component) สำหรับ session และ interactivity
 */
export default function HomePage() {
  return <HomeClient />;
}
