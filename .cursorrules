## 🚀 เริ่มต้นใช้งาน
```cmd
# ติดตั้ง dependencies ทั้งหมด
npm run install:all

# รัน development servers
npm run dev
```

## 🌐 URLs สำหรับการพัฒนา
- Frontend: http://localhost:3000
- API Gateway: http://localhost:3001
- Auth Service: http://localhost:3002
- User Service: http://localhost:3003
- Database Service: http://localhost:3004

## 🏗️ สถาปัตยกรรมระบบ
- **Microservices** - แยก services ออกจากกัน
- **Frontend**: Next.js + TypeScript
- **Backend**: Express.js services
- **Database**: SQLite + Prisma ORM (เฉพาะ Auth Service)
- **Authentication**: NextAuth JWT + LDAP
- **API Gateway**: จัดการ routing และ forwarding ไปยัง services

## 📁 โครงสร้างโปรเจค
```
portalrpp/
├── frontend/                 # Next.js Frontend
├── backend/
│   ├── api-gateway/         # API Gateway Service
│   ├── auth-service/        # Authentication Service
│   └── [user-service]/      # User Management Service (อนาคต)
├── scripts/                 # Utility Scripts
└── package.json             # Root package.json
```

## 🛠️ การพัฒนา
### การรันโปรเจค
- ใช้ **local development** (รัน services แยกกัน)
- รันด้วย `npm run dev` ในแต่ละ service
- ใช้ localhost URLs ในการสื่อสาร
- ใช้ **Windows Command Prompt (cmd)** เป็นมาตรฐาน

### Windows Development Commands
- **ใช้คำสั่ง cmd เป็นมาตรฐาน** สำหรับการพัฒนาใน Windows
- ใช้ `cmd` แทน `bash` หรือ `powershell` สำหรับคำสั่ง terminal
- ใช้ `dir` แทน `ls` สำหรับการแสดงรายการไฟล์
- ใช้ `copy` แทน `cp` สำหรับการคัดลอกไฟล์
- ใช้ `move` แทน `mv` สำหรับการย้ายไฟล์
- ใช้ `del` แทน `rm` สำหรับการลบไฟล์
- ใช้ `mkdir` สำหรับการสร้างโฟลเดอร์
- ใช้ `cd` สำหรับการเปลี่ยนไดเรกทอรี
- ใช้ `netstat -ano | findstr :PORT` สำหรับการตรวจสอบ port ที่ใช้งาน
- ใช้ `taskkill /PID <PID>` สำหรับการหยุด process
- ใช้ `tasklist` สำหรับการแสดงรายการ processes
- ใช้ `ipconfig` สำหรับการตรวจสอบ network configuration
- ใช้ `ping` สำหรับการทดสอบการเชื่อมต่อ
- ใช้ `nslookup` สำหรับการตรวจสอบ DNS
- ใช้ `tracert` แทน `traceroute` สำหรับการติดตามเส้นทาง
- ใช้ `systeminfo` สำหรับข้อมูลระบบ
- ใช้ `ver` สำหรับเวอร์ชันของ Windows
- ใช้ `echo %PATH%` สำหรับการแสดง PATH environment variable
- ใช้ `set` สำหรับการแสดง environment variables
- ใช้ `set VARIABLE=value` สำหรับการตั้งค่า environment variable
- ใช้ `cls` สำหรับการล้างหน้าจอ
- ใช้ `help` สำหรับการแสดงคำสั่งที่ใช้ได้
- ใช้ `/?` หลังคำสั่งเพื่อดู help (เช่น `dir /?`)

#### 🔍 การตรวจสอบก่อนรัน
- **ตรวจสอบ port ที่ใช้งาน** ก่อนรัน service ใหม่
- ใช้คำสั่ง `netstat -ano | findstr :3000` สำหรับการตรวจสอบ port
- ตรวจสอบ ports: 3000, 3001, 3002, 3003, 3004
- **หยุด service เดิม** ก่อนรันใหม่ หาก port ถูกใช้งาน
- ใช้ `taskkill /PID <PID>` เพื่อหยุด process

#### ⚠️ กฎการรัน
- **ห้ามรัน service ซ้ำ** หากกำลังรันอยู่แล้ว
- **ตรวจสอบ health check** ก่อนรัน service ใหม่
- ใช้ endpoint `/health` เพื่อตรวจสอบสถานะ service
- **รอให้ service เริ่มต้นเสร็จ** ก่อนรัน service ถัดไป
- **ตรวจสอบ logs** หากเกิดปัญหาในการรัน

### รูปแบบโค้ด
- ใช้ **TypeScript** ทุก service
- ใช้ **ESLint** และ **Prettier**
- ใช้ **ภาษาไทย** ใน comments
- ใช้ **semantic commit messages**

### Environment Variables
- ใช้ `.env` files แต่ละ service
- ใช้ `env.example` templates
- ใช้ localhost URLs

## 🔐 ความปลอดภัย
- ใช้ **JWT** สำหรับ authentication
- รองรับ **LDAP** (Windows Active Directory)
- ใช้ **SQLite** สำหรับ development (เฉพาะ Auth Service)
- ใช้ **Prisma ORM** สำหรับ database (เฉพาะ Auth Service)
- **Database Service ถูกแยกออก** และเข้าถึงได้ผ่าน API Gateway เท่านั้น

## 📊 การ Monitor
- Health check endpoints ทุก service
- Logging ที่เหมาะสม
- Error handling ที่ครอบคลุม

## 🧹 กฎการจัดการไฟล์
- **ลบไฟล์ทดสอบทันที** หลังใช้งาน
- **ไม่สร้าง README.md** อธิบายการทำงาน
- ใช้ **comments ในโค้ด** แทนเอกสารแยก

## 🪟 Windows Development Standards
- **ใช้คำสั่ง cmd เป็นมาตรฐาน** สำหรับการพัฒนาใน Windows
- ใช้ `cmd` แทน `bash` หรือ `powershell` สำหรับคำสั่ง terminal
- ใช้ `dir` แทน `ls` สำหรับการแสดงรายการไฟล์
- ใช้ `copy` แทน `cp` สำหรับการคัดลอกไฟล์
- ใช้ `move` แทน `mv` สำหรับการย้ายไฟล์
- ใช้ `del` แทน `rm` สำหรับการลบไฟล์
- ใช้ `mkdir` สำหรับการสร้างโฟลเดอร์
- ใช้ `cd` สำหรับการเปลี่ยนไดเรกทอรี
- ใช้ `netstat -ano | findstr :PORT` สำหรับการตรวจสอบ port ที่ใช้งาน
- ใช้ `taskkill /PID <PID>` สำหรับการหยุด process
- ใช้ `tasklist` สำหรับการแสดงรายการ processes
- ใช้ `ipconfig` สำหรับการตรวจสอบ network configuration
- ใช้ `ping` สำหรับการทดสอบการเชื่อมต่อ
- ใช้ `nslookup` สำหรับการตรวจสอบ DNS
- ใช้ `tracert` แทน `traceroute` สำหรับการติดตามเส้นทาง
- ใช้ `systeminfo` สำหรับข้อมูลระบบ
- ใช้ `ver` สำหรับเวอร์ชันของ Windows
- ใช้ `echo %PATH%` สำหรับการแสดง PATH environment variable
- ใช้ `set` สำหรับการแสดง environment variables
- ใช้ `set VARIABLE=value` สำหรับการตั้งค่า environment variable
- ใช้ `cls` สำหรับการล้างหน้าจอ
- ใช้ `help` สำหรับการแสดงคำสั่งที่ใช้ได้
- ใช้ `/?` หลังคำสั่งเพื่อดู help (เช่น `dir /?`)

### 🔗 การเรียกใช้งาน Service
- **Frontend ต้องเรียกผ่าน API Gateway เท่านั้น** - ห้ามเรียก service โดยตรง
- ใช้ URL: `http://localhost:3001` สำหรับทุก API calls
- ตัวอย่าง: `/api/users` → `http://localhost:3001/api/users`
- **ห้ามเรียก**: `http://localhost:3002`, `http://localhost:3003`, `http://localhost:3004` โดยตรง
- **API Gateway จะจัดการ routing** ไปยัง service ที่เหมาะสมโดยอัตโนมัติ

### 🗄️ การเรียกใช้งาน Database
- **Backend services ต้องเรียกผ่าน API Gateway เท่านั้น** - ห้ามเรียก Database Service โดยตรง
- ใช้ URL: `http://localhost:3001` สำหรับ database operations
- ตัวอย่าง: `/api/db/users` → `http://localhost:3001/api/db/users`
- **ห้ามเรียก**: `http://localhost:3004` โดยตรงจาก service อื่น
- **API Gateway จะ forward requests ไปยัง Database Service** โดยอัตโนมัติ

## 📁 โครงสร้าง Service (ตาม auth-service)
```
backend/auth-service/
├── src/
│   ├── index.ts              # Main entry point
│   ├── routes/               # API routes
│   │   ├── authRoutes.ts     # Authentication routes
│   │   └── adminRoutes.ts    # Admin routes
│   ├── services/             # Business logic
│   │   ├── authService.ts    # Authentication service
│   │   ├── ldapService.ts    # LDAP service
│   │   └── adminService.ts   # Admin service
│   ├── config/               # Configuration files
│   │   ├── auth.ts          # Auth configuration
│   │   ├── database.ts      # Database configuration
│   │   └── ldap.ts          # LDAP configuration
│   ├── middleware/           # Express middleware
│   │   ├── authMiddleware.ts # Authentication middleware
│   │   ├── rateLimitMiddleware.ts # Rate limiting
│   │   └── validationMiddleware.ts # Request validation
│   ├── utils/                # Utility functions
│   │   ├── errorHandler.ts   # Error handling
│   │   ├── logger.ts         # Logging utilities
│   │   └── validation.ts     # Validation utilities
│   └── types/                # TypeScript type definitions
│       └── index.ts          # Type definitions
├── prisma/                   # Database schema & migrations
│   ├── schema.prisma         # Prisma schema
│   ├── seed.ts              # Database seeding
│   └── migrations/           # Database migrations
├── public/                   # Static files
│   ├── auth-test.html        # Test interface
│   ├── auth-test.js          # Test JavaScript
│   └── styles.css            # Styles
├── package.json              # Dependencies & scripts
├── tsconfig.json             # TypeScript configuration
├── eslint.config.js          # ESLint configuration
├── .prettierrc              # Prettier configuration
├── env.example              # Environment variables template
└── README.md                # Service documentation
```

## 🛠️ Scripts ที่ใช้บ่อย
```cmd
# Development
npm run dev                  # รัน development server
npm run start:dev           # รันด้วย tsx

# Build & Production
npm run build               # Build TypeScript
npm run build:prod          # Build for production
npm run start               # รัน production server

# Code Quality
npm run lint                # ตรวจสอบ code style
npm run lint:fix            # แก้ไข code style อัตโนมัติ
npm run type-check          # ตรวจสอบ TypeScript types
npm run quality-check       # ตรวจสอบคุณภาพโค้ด
npm run format              # จัดรูปแบบโค้ดด้วย Prettier

# Database
npm run db:generate         # Generate Prisma client
npm run db:push             # Push schema to database
npm run db:migrate          # Run database migrations
npm run db:studio           # Open Prisma Studio
npm run db:reset            # Reset database
npm run db:seed             # Seed database
```
