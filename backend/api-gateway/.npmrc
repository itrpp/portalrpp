# Node.js v23 compatibility settings
engine-strict=false

# Use legacy peer deps for compatibility
legacy-peer-deps=true

# Use prefer-offline instead of deprecated cache-min
prefer-offline=true

# Suppress engine warnings
loglevel=error
