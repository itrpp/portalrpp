# ========================================
# Cursor Rules สำหรับ Revenue Service (โรงพยาบาล)
# ========================================

## 🎯 เป้าหมายหลัก
Revenue Service เป็น service สำหรับฝ่ายการจัดเก็บรายได้ของโรงพยาบาล รับผิดชอบการจัดการรายได้ การส่งเบิกเคลมค่าใช้จ่ายกับหน่วยงานต่างๆ การจัดการข้อมูลทางการเงิน และการสร้างรายงานทางการเงินของโรงพยาบาล ต้องมีความแม่นยำ ตรวจสอบได้ และรองรับการประมวลผลข้อมูลจำนวนมาก

## 🏥 Hospital Revenue Management

### หลักการออกแบบ
- **Claim Processing**: การประมวลผลการเบิกเคลมค่าใช้จ่าย
- **Insurance Integration**: การเชื่อมต่อกับประกันสุขภาพ
- **Government Agency Integration**: การเชื่อมต่อกับหน่วยงานรัฐ
- **Patient Billing**: การเรียกเก็บเงินจากผู้ป่วย
- **Revenue Tracking**: การติดตามรายได้
- **Compliance Management**: การจัดการตามกฎระเบียบ

### Revenue Processing Flow
```
Patient Visit → Service Provided → Claim Generation → Insurance/Government → Payment Processing → Revenue Recording
                ↓
            Processing Steps:
            - Service Documentation
            - Claim Preparation
            - Insurance Verification
            - Government Agency Submission
            - Payment Tracking
            - Revenue Reconciliation
```

## 📊 Data Management

### Data Structure
- ใช้ normalized database design สำหรับข้อมูลโรงพยาบาล
- เก็บข้อมูลการเบิกเคลมแยกตามประเภท (ประกัน, หน่วยงานรัฐ, ผู้ป่วย)
- เก็บประวัติการเปลี่ยนแปลงข้อมูล
- ใช้ proper indexing สำหรับ performance
- จัดการ data relationships ระหว่างผู้ป่วย บริการ และการชำระเงิน

### Data Validation
- ตรวจสอบความถูกต้องของข้อมูลการเบิกเคลม
- ตรวจสอบ business rules ของโรงพยาบาล
- ตรวจสอบ data integrity
- ตรวจสอบ calculation accuracy
- ตรวจสอบ date ranges และ service periods

### Data Processing
- ประมวลผลข้อมูลการเบิกเคลมแบบ batch และ real-time
- ใช้ caching สำหรับ performance
- จัดการ data aggregation สำหรับรายงาน
- ใช้ proper error handling
- เก็บ processing logs

## 📈 Reporting System

### Report Types
- **Daily Revenue Reports**: รายงานรายได้รายวัน
- **Monthly Revenue Reports**: รายงานรายได้รายเดือน
- **Claim Status Reports**: รายงานสถานะการเบิกเคลม
- **Insurance Payment Reports**: รายงานการชำระเงินจากประกัน
- **Government Agency Reports**: รายงานการเบิกเคลมหน่วยงานรัฐ
- **Patient Payment Reports**: รายงานการชำระเงินจากผู้ป่วย

### Report Features
- Export to multiple formats (PDF, Excel, CSV)
- Real-time data updates
- Interactive charts and graphs
- Drill-down capabilities
- Scheduled report generation
- Claim tracking visualization

### Report Security
- Role-based access control
- Data encryption
- Audit logging
- Secure file storage
- Access monitoring

## 🧮 Calculation Engine

### Revenue Calculations
- **Gross Revenue**: รายได้รวมจากทุกแหล่ง
- **Net Revenue**: รายได้สุทธิหลังหักค่าใช้จ่าย
- **Insurance Claims**: การเบิกเคลมประกันสุขภาพ
- **Government Claims**: การเบิกเคลมหน่วยงานรัฐ
- **Patient Payments**: การชำระเงินจากผู้ป่วย
- **Co-payment Calculations**: การคำนวณส่วนร่วมจ่าย

### Calculation Rules
- ใช้ business rules engine สำหรับโรงพยาบาล
- รองรับ multiple calculation methods
- ตรวจสอบ calculation accuracy
- เก็บ calculation history
- รองรับ rule updates

### Performance Optimization
- ใช้ caching strategies
- Optimize database queries
- ใช้ parallel processing
- ใช้ batch processing
- Monitor performance metrics

## 🔐 Security & Compliance

### Data Security
- ใช้ encryption สำหรับข้อมูลสำคัญของโรงพยาบาล
- ใช้ secure communication protocols
- จัดการ access control
- ใช้ audit logging
- ใช้ data masking

### Healthcare Compliance
- ตรวจสอบ regulatory requirements ของโรงพยาบาล
- เก็บ audit trails
- ใช้ proper data retention
- ตรวจสอบ data accuracy
- ใช้ compliance reporting

### Access Control
- Role-based permissions
- Data-level security
- Session management
- IP restrictions
- Activity monitoring

## 🔄 API Design

### RESTful Endpoints
- ใช้ HTTP methods ที่เหมาะสม
- ตั้งค่า proper status codes
- ใช้ consistent response format
- จัดการ error responses
- ใช้ proper URL structure

### Request/Response Format
```typescript
// Claim Submission Request
{
  "patientId": "P12345",
  "serviceDate": "2024-01-15",
  "serviceType": "consultation",
  "insuranceProvider": "SSO",
  "claimAmount": 1500,
  "diagnosisCode": "ICD-10-CM",
  "treatmentCodes": ["CPT-001", "CPT-002"]
}

// Claim Response
{
  "success": true,
  "data": {
    "claimId": "CLM-2024-001",
    "status": "submitted",
    "submittedAmount": 1500,
    "approvedAmount": 1200,
    "rejectedAmount": 300,
    "processingTime": "5-7 days"
  },
  "metadata": {
    "submittedAt": "2024-01-15T10:30:00Z",
    "insuranceProvider": "SSO",
    "claimType": "outpatient"
  }
}
```

### Error Handling
- ใช้ consistent error format
- จัดการ different error types
- ใช้ proper HTTP status codes
- Log errors appropriately
- ไม่ expose sensitive information

## 📊 Data Analytics

### Analytics Features
- **Revenue Trend Analysis**: วิเคราะห์แนวโน้มรายได้
- **Claim Performance Analysis**: วิเคราะห์ประสิทธิภาพการเบิกเคลม
- **Insurance Provider Analysis**: วิเคราะห์ประสิทธิภาพประกัน
- **Government Agency Analysis**: วิเคราะห์ประสิทธิภาพหน่วยงานรัฐ
- **Patient Payment Analysis**: วิเคราะห์การชำระเงินจากผู้ป่วย

### Data Visualization
- Interactive charts
- Real-time dashboards
- Custom visualizations
- Export capabilities
- Mobile-responsive design

### Business Intelligence
- Data mining capabilities
- Predictive analytics
- Statistical analysis
- Custom algorithms
- Machine learning integration

## 🧪 Testing Strategy

### Unit Tests
- ทดสอบ calculation functions
- ทดสอบ data validation
- ทดสอบ business rules
- ทดสอบ report generation
- ทดสอบ API endpoints

### Integration Tests
- ทดสอบ data processing
- ทดสอบ report generation
- ทดสอบ calculation accuracy
- ทดสอบ performance
- ทดสอบ error scenarios

### Data Tests
- ทดสอบ data integrity
- ทดสอบ calculation accuracy
- ทดสอบ business rules
- ทดสอบ audit trails
- ทดสอบ compliance

## 📝 Code Quality

### TypeScript Best Practices
- ใช้ strict mode
- กำหนด interfaces ให้ชัดเจน
- ใช้ proper type annotations
- ใช้ enums สำหรับ constants
- ใช้ generics เมื่อเหมาะสม

### Error Handling
- ใช้ custom error classes
- จัดการ async errors
- ใช้ try-catch blocks
- Log errors with context
- ใช้ error boundaries

### Code Organization
- แยก business logic
- ใช้ dependency injection
- แยก concerns
- ใช้ proper naming conventions
- ใช้ consistent formatting

## 🔧 Configuration Management

### Environment Variables
- ใช้ `.env` files
- แยก config สำหรับ environments
- ใช้ validation สำหรับ config
- ใช้ default values
- ใช้ secure secrets management

### Database Configuration
- ใช้ connection pooling
- ตั้งค่า timeout ที่เหมาะสม
- ใช้ SSL ใน production
- จัดการ database migrations
- ใช้ database monitoring

### Calculation Configuration
- ใช้ configurable business rules
- ใช้ insurance rate configuration
- ใช้ government agency configuration
- ใช้ currency configuration
- ใช้ exchange rate configuration

## 📈 Monitoring และ Logging

### Logging Strategy
- ใช้ structured logging
- Log calculation events
- Log data processing events
- Log report generation events
- ใช้ log levels ที่เหมาะสม

### Metrics
- Revenue calculation accuracy
- Report generation performance
- Data processing throughput
- API response times
- Error rates

### Alerts
- Alert เมื่อ calculation errors
- Alert เมื่อ data processing failures
- Alert เมื่อ report generation failures
- Alert เมื่อ performance degradation
- Alert เมื่อ data integrity issues

## 📊 Performance Monitoring

### Calculation Performance
- Monitor calculation speed
- Track processing times
- Monitor memory usage
- Track CPU utilization
- Monitor database performance

### Report Performance
- Monitor report generation time
- Track export performance
- Monitor chart rendering
- Track data loading times
- Monitor user experience

### System Performance
- Monitor API response times
- Track database query performance
- Monitor memory usage
- Track disk I/O
- Monitor network performance

## 🔄 Data Processing

### Batch Processing
- Process large datasets
- Use background jobs
- Handle processing errors
- Monitor job progress
- Clean up completed jobs

### Real-time Processing
- Process streaming data
- Handle real-time calculations
- Update dashboards
- Send notifications
- Handle data updates

### Data Aggregation
- Aggregate daily data
- Aggregate monthly data
- Aggregate yearly data
- Handle data rollups
- Manage data summaries

## 📋 API Documentation

### OpenAPI/Swagger
- สร้าง OpenAPI specification
- ใช้ Swagger UI
- อัปเดต documentation
- รวม examples
- ใช้ proper schemas

### API Versioning
- รองรับ API versions
- ใช้ URL versioning
- จัดการ backward compatibility
- แยก documentation
- ใช้ version headers

## 🛠️ Development Workflow

### Local Development
- ใช้ SQLite สำหรับ development
- ใช้ sample data
- ใช้ hot reload
- ใช้ debug mode
- ใช้ local environment
- ใช้ **Windows Command Prompt (cmd)** เป็นมาตรฐาน

### Windows Development Commands
- **ใช้คำสั่ง cmd เป็นมาตรฐาน** สำหรับการพัฒนาใน Windows
- ใช้ `cmd` แทน `bash` หรือ `powershell` สำหรับคำสั่ง terminal
- ใช้ `dir` แทน `ls` สำหรับการแสดงรายการไฟล์
- ใช้ `copy` แทน `cp` สำหรับการคัดลอกไฟล์
- ใช้ `move` แทน `mv` สำหรับการย้ายไฟล์
- ใช้ `del` แทน `rm` สำหรับการลบไฟล์
- ใช้ `mkdir` สำหรับการสร้างโฟลเดอร์
- ใช้ `cd` สำหรับการเปลี่ยนไดเรกทอรี
- ใช้ `netstat -ano | findstr :PORT` สำหรับการตรวจสอบ port ที่ใช้งาน
- ใช้ `taskkill /PID <PID>` สำหรับการหยุด process
- ใช้ `tasklist` สำหรับการแสดงรายการ processes
- ใช้ `ipconfig` สำหรับการตรวจสอบ network configuration
- ใช้ `ping` สำหรับการทดสอบการเชื่อมต่อ
- ใช้ `nslookup` สำหรับการตรวจสอบ DNS
- ใช้ `tracert` แทน `traceroute` สำหรับการติดตามเส้นทาง
- ใช้ `systeminfo` สำหรับข้อมูลระบบ
- ใช้ `ver` สำหรับเวอร์ชันของ Windows
- ใช้ `echo %PATH%` สำหรับการแสดง PATH environment variable
- ใช้ `set` สำหรับการแสดง environment variables
- ใช้ `set VARIABLE=value` สำหรับการตั้งค่า environment variable
- ใช้ `cls` สำหรับการล้างหน้าจอ
- ใช้ `help` สำหรับการแสดงคำสั่งที่ใช้ได้
- ใช้ `/?` หลังคำสั่งเพื่อดู help (เช่น `dir /?`)

### Code Quality Tools
- ใช้ ESLint
- ใช้ Prettier
- ใช้ TypeScript strict mode
- ใช้ Husky hooks
- ใช้ commitlint

## 🔍 Debugging และ Troubleshooting

### Calculation Debugging
- Debug calculation errors
- Debug data validation issues
- Debug business rule problems
- Debug performance issues
- Debug accuracy problems

### Report Debugging
- Debug report generation
- Debug data aggregation
- Debug chart rendering
- Debug export issues
- Debug performance problems

### Data Debugging
- Debug data integrity issues
- Debug data processing errors
- Debug calculation accuracy
- Debug audit trail problems
- Debug compliance issues

## 📚 Best Practices Summary

### Do's ✅
- ใช้ proper data validation
- ใช้ business rules engine
- ใช้ audit logging
- ใช้ performance optimization
- ใช้ error handling
- ใช้ TypeScript strict mode
- ใช้ environment variables
- ใช้ security measures
- ใช้ data encryption
- ใช้ compliance standards

### Don'ts ❌
- อย่าลืม data validation
- อย่าลืม error handling
- อย่าลืม audit logging
- อย่าลืม performance optimization
- อย่าลืม security measures
- อย่าลืม compliance requirements
- อย่าลืม data encryption
- อย่าลืม business rules
- อย่าลืม calculation accuracy
- อย่าลืม data integrity

## 🎯 Performance Targets

### Response Time
- Revenue calculation: < 1000ms
- Report generation: < 5000ms
- Data aggregation: < 2000ms
- API responses: < 500ms
- Chart rendering: < 1000ms

### Throughput
- Revenue calculations: > 1000/second
- Report generations: > 100/hour
- Data processing: > 10000/second
- API requests: > 1000/second
- Export operations: > 50/minute

### Accuracy
- Calculation accuracy: > 99.99%
- Data integrity: > 99.99%
- Report accuracy: > 99.99%
- Audit trail accuracy: > 99.99%
- Compliance accuracy: > 99.99%

## 🏥 Hospital Business Requirements

### Revenue Types
- **Insurance Claims**: การเบิกเคลมประกันสุขภาพ
- **Government Claims**: การเบิกเคลมหน่วยงานรัฐ
- **Patient Payments**: การชำระเงินจากผู้ป่วย
- **Co-payments**: ส่วนร่วมจ่าย
- **Service Fees**: ค่าบริการ

### Insurance Providers
- **SSO (Social Security Office)**: สำนักงานประกันสังคม
- **CSMBS (Civil Servant Medical Benefit Scheme)**: สวัสดิการข้าราชการ
- **Private Insurance**: ประกันเอกชน
- **Universal Coverage**: หลักประกันสุขภาพถ้วนหน้า
- **Other Insurance**: ประกันอื่นๆ

### Government Agencies
- **Ministry of Public Health**: กระทรวงสาธารณสุข
- **Provincial Health Office**: สำนักงานสาธารณสุขจังหวัด
- **District Health Office**: สำนักงานสาธารณสุขอำเภอ
- **Local Administrative Organization**: องค์กรปกครองส่วนท้องถิ่น
- **Other Government Agencies**: หน่วยงานรัฐอื่นๆ

### Claim Types
- **Outpatient Claims**: การเบิกเคลมผู้ป่วยนอก
- **Inpatient Claims**: การเบิกเคลมผู้ป่วยใน
- **Emergency Claims**: การเบิกเคลมฉุกเฉิน
- **Surgery Claims**: การเบิกเคลมการผ่าตัด
- **Laboratory Claims**: การเบิกเคลมห้องปฏิบัติการ
- **Pharmacy Claims**: การเบิกเคลมยา
- **Medical Device Claims**: การเบิกเคลมอุปกรณ์การแพทย์ 