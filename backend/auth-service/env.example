# Auth Service Environment Variables

# Server Configuration
PORT=3002
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your-secret-key-here
REFRESH_TOKEN_SECRET=your-refresh-secret-key-here
NEXTAUTH_SECRET=your-nextauth-secret-here

# Token Expiry Settings
ACCESS_TOKEN_EXPIRY=7d        # Access token หมดอายุใน 7 วัน
REFRESH_TOKEN_EXPIRY=30d      # Refresh token หมดอายุใน 30 วัน

# Database Configuration (สำหรับอนาคต)
# DATABASE_URL=postgresql://username:password@localhost:5432/rpp_portal

# Redis Configuration (สำหรับอนาคต)
# REDIS_URL=redis://localhost:6379

# Email Configuration (สำหรับอนาคต)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Windows LDAP Configuration (Direct Username Binding)
# สำหรับ Windows Active Directory/LDAP Server ที่ไม่ใช้ DN format
LDAP_URL=ldap://192.168.238.8:389

# User Search Configuration (สำหรับค้นหาข้อมูลผู้ใช้หลัง authentication)
LDAP_USER_SEARCH_BASE=cn=Users,dc=domain,dc=local
LDAP_USERNAME_ATTRIBUTE=sAMAccountName

# Test User Credentials (สำหรับทดสอบ)
LDAP_USERNAME=arin
LDAP_USER_PASSWORD=o6Udojkiydot

# Optional LDAP Group Configuration for Windows AD
# LDAP_GROUPS_SEARCH_BASE=cn=Users,dc=domain,dc=local
# LDAP_GROUP_CLASS=group
# LDAP_GROUP_MEMBER_ATTRIBUTE=member

# หมายเหตุ: การตั้งค่าใหม่นี้ใช้ Direct Username Binding
# - ไม่ต้องใช้ LDAP_ADMIN_DN และ LDAP_ADMIN_PASSWORD
# - ระบบจะใช้ username ที่ผู้ใช้ป้อนเข้ามาใน bind โดยตรง
# - หากไม่สำเร็จจะลองเพิ่ม @domain.local ต่อท้าย username
# - LDAP_USER_SEARCH_BASE ใช้สำหรับค้นหาข้อมูลผู้ใช้เพิ่มเติมเท่านั้น
# 
# การตั้งค่าที่ต้องปรับแต่ง:
# - LDAP_URL -> เปลี่ยนเป็น IP และ port ของ Windows LDAP server
# - dc=domain,dc=local -> เปลี่ยนเป็น domain ที่ใช้จริง
# - LDAP_USERNAME_ATTRIBUTE -> ปกติใช้ sAMAccountName สำหรับ Windows AD 